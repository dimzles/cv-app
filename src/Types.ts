export type GeneralInfoData = {
    fullName: string,
    email: string,
    phoneNumber: string,
    userLocation: string
}

export type EducationInfoData = {
    university: string,
    startDate: string,
    endDate: string,
    description: string,
}